import React from 'react';
import ShapeSimulation from './settings/ShapeSimulation';

const ShapeSync = () => {
  return (
    <div className="w-full h-screen">
      <ShapeSimulation />
    </div>
  );
};

export default ShapeSync;
